spring.application.name=backend
server.port=${PORT:8080}
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:rituals}?sslmode=require
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:shivudb}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT JWT
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# Mail (Gmail SMTP configuration)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# USER ACTION REQUIRED: Put your Gmail address and 16-character App Password here:
spring.mail.username=${MAIL_USERNAME:shivanimanivannan1@gmail.com}
spring.mail.password=${MAIL_PASSWORD:YOUR_16_CHAR_APP_PASSWORD_HERE}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
